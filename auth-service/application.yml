server:
  port: 8081
spring:
  application:
    name: auth-service
eureka:
  client:
    serviceUrl:
      defaultZone: '${EUREKA_DEFAULT_ZONE:http\://discovery-service\:8761/eureka/}'
management:
  endpoints:
    web:
      exposure:
        include: health,env
