server:
  port: 8081
spring:
  application:
    name: auth-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-service:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: health,env
jwt:
  secret-key: mySecretKey1234567890
