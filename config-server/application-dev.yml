# Development profile for config-server
server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///workspaces/spring-cloud-example/config-repo
          skipSslValidation: true
eureka:
  client:
    enabled: false
